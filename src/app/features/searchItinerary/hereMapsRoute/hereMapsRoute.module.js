import angular from 'angular';
import component from './hereMapsRoute.component';

const hereMapsRouteModule = angular.module('hereMapsRoute', [
])
	.component('hereMapsRoute', component);

export default hereMapsRouteModule;
